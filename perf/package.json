{
  "name": "perf",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "artillery": "artillery",
    "api": "artillery run rates.yaml -e api",
    "rates": "artillery run rates.yaml -e api",
    "exchange-concurrency": "artillery run exchange-concurrency.yaml -e api",
    "exchange-concurrency-stress": "artillery run exchange-concurrency-stress.yaml -e api",
    "exchange-concurrency-overdraft": "artillery run exchange-concurrency-overdraft.yaml -e api",
    "scenario": "./run-scenario.sh"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "artillery": "2.0.22",
    "artillery-plugin-statsd": "^2.2.1"
  }
}
